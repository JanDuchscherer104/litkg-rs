# Repo Layout

- `crates/litkg-core`: normalized paper model and pipeline primitives
- `crates/litkg-cli`: operator-facing CLI
- `crates/litkg-graphify`: graphify materialization adapter
- `crates/litkg-neo4j`: optional Neo4j export adapter
- `examples/`: client repo configs
- `.agents/`: local backlog and agent context
